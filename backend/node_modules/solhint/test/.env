/////////////////////////////////////////////////////////////////////////
DOCKER:
/////////////////////////////////////////////////////////////////////////

BUILD THE IMAGE:
/////////////////////////////////////////////////////////////////////////
1. In the folder where Dockerfile is stored run (docker/Dockerfile):
docker buildx build -t protofire/solhint .
OR directly
docker buildx build -t protodb/protofire-solhint:latest .

2. When doing: docker images
It shows as protofire/solhint


PUSH TO DOCKER HUB:
/////////////////////////////////////////////////////////////////////////
1. Run 
docker login -u protodb

2. At the password prompt, enter the personal access token.
dckr_pat_Po3KpfXLxDiBt_R2bbRQWUM6rEg
(ask for password [lektroS])

3. Docker Hub needs the first part to be user name so run (if username is not part of the name):
docker tag protofire/solhint:latest protodb/protofire-solhint:latest

4. Push into Docker Hub
docker push protodb/protofire-solhint:latest


